<div>
  <h1>Tasks</h1>
  <% @tasks.each do |task| %>
  <%= link_to task[:title], "/tasks/#{task[:id]}" %>
  <%= link_to "Delete",
            task_path(task),
            method: :delete,
            data: { confirm: "Are you sure?" } %><br>
  <% end %>
</div>
